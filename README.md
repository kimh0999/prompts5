# prompts5
프롬포트 관리 프로그램

---

## 프로젝트 목적

자주 쓰는 생성형 AI 프롬프트를 매번 다시 쓰거나 여기저기 흩어진 메모에서 찾는 대신,
한곳에 모아 두고 **목적에 따라 분류하고 빠르게 찾아 쓰기 위한** 콘솔 프로그램입니다.

Python 기본 문법과 Git 사용법을 익히기 위한 학습용 프로젝트이며,
외부 패키지 없이 Python 표준 기능만 사용합니다.

## 주요 기능

터미널에서 번호를 입력해 기능을 선택합니다.

| 번호 | 기능 | 설명 |
|---|---|---|
| 1 | 프롬프트 추가 | 제목·목적·카테고리·내용·태그·모델을 입력받아 등록 |
| 2 | 전체 목록 보기 | 등록된 프롬프트를 표로 요약 표시 |
| 3 | 카테고리별 조회 | 카테고리를 골라 해당 프롬프트만 표시 |
| 4 | 키워드 검색 | 제목·목적·내용·태그에서 검색 (대소문자 구분 없음) |
| 5 | 프롬프트 상세 보기 | 번호로 골라 모든 정보와 내용 전문 표시 |
| 6 | 즐겨찾기 추가·해제 | 즐겨찾기 상태를 켜고 끔 |
| 7 | 즐겨찾기 목록 보기 | 즐겨찾기한 프롬프트만 모아서 표시 |
| 0 | 프로그램 종료 | |

기능을 실행한 뒤에는 항상 메인 메뉴로 돌아옵니다.

### 잘못된 입력 처리

아래 상황에서 프로그램이 종료되지 않고 안내 메시지를 출력한 뒤 메뉴로 돌아옵니다.

- 메뉴에 없는 번호 (`9`, `-1` 등)
- 숫자가 아닌 입력 (`abc`)
- 존재하지 않는 프롬프트 번호 (`999`)
- 제목·목적·내용 등의 빈 입력
- 존재하지 않는 카테고리 선택

`Ctrl+C`로 중단해도 오류 화면 없이 정상 종료됩니다.

## 데이터 구조

프롬프트는 **리스트 안에 딕셔너리**를 담는 형태로 메모리에 저장합니다.

```python
{
    "id": 1,
    "title": "IT 뉴스 요약",
    "category": "요약·분석",
    "purpose": "IT 뉴스의 핵심 내용과 기술 동향 파악",
    "content": "실제로 복사하여 사용할 프롬프트 내용",
    "tags": ["IT", "뉴스", "요약"],
    "model": "ChatGPT",
    "favorite": False
}
```

| 항목 | 자료형 | 의미 |
|---|---|---|
| `id` | `int` | 프롬프트를 구분하는 번호 (기존 최대값 + 1로 자동 부여) |
| `title` | `str` | 프롬프트 이름 |
| `category` | `str` | 사용 목적에 따른 분류 |
| `purpose` | `str` | 프롬프트를 사용하는 이유 |
| `content` | `str` | 실제 프롬프트 내용 (여러 줄 가능) |
| `tags` | `list[str]` | 검색에 사용할 세부 키워드 |
| `model` | `str` | 사용할 생성형 AI |
| `favorite` | `bool` | 즐겨찾기 여부 |

프로그램을 시작하면 기본 프롬프트 4개가 등록되어 있습니다.

> 데이터는 **메모리에만 저장**되므로 프로그램을 종료하면 실행 중 추가·변경한 내용은 사라집니다.
> 파일 저장(JSON)은 이번 범위에 포함되지 않습니다.

## 카테고리 목록

결과물의 형태가 아니라 **사용 목적**을 기준으로 분류합니다.

```python
CATEGORIES = [
    "요약·분석",
    "개발·코딩",
    "기획·문서화",
    "콘텐츠 제작",
    "업무 자동화",
    "학습·교육",
    "취업·커리어",
    "기타"
]
```

목록에서 번호로 선택하거나, `0`을 눌러 목록에 없는 카테고리를 직접 입력할 수도 있습니다.

## 프로젝트 파일 구조

```
python-prompt/
├── .gitignore      # Git이 추적하지 않을 파일 목록
├── README.md       # 이 문서
└── main.py         # 프로그램 전체 (약 410줄)
```

`main.py`의 함수 구성은 다음과 같습니다.

| 함수 | 역할 |
|---|---|
| `create_default_prompts()` | 기본 프롬프트 4개 생성 |
| `get_non_empty_input()` | 빈 값을 거부하는 입력 |
| `get_multiline_input()` | `END`가 나올 때까지 여러 줄 입력 |
| `select_category()` | 카테고리 선택 또는 직접 입력 |
| `add_prompt()` | 프롬프트 추가 |
| `text_width()`, `fit()` | 한글 폭을 고려한 표 정렬과 말줄임 |
| `print_prompt_table()` | 목록 표 출력 (4개 기능이 공유) |
| `show_prompt_list()` | 전체 목록 |
| `show_by_category()` | 카테고리별 조회 |
| `search_prompts()` | 키워드 검색 |
| `find_prompt_by_id()` | 번호 입력·검증·조회 (2개 기능이 공유) |
| `show_prompt_detail()` | 상세 보기 |
| `toggle_favorite()` | 즐겨찾기 추가·해제 |
| `show_favorites()` | 즐겨찾기 목록 |
| `show_menu()`, `main()` | 메뉴 출력과 반복 루프 |

## Python 요구 버전

**Python 3.10 이상**이 필요합니다. 메뉴 분기에 3.10부터 추가된 `match / case` 문법을 사용합니다.

## 실행 방법

```bash
py -3.13 main.py
```

버전 확인:

```bash
py -3.13 --version
```

> **주의**: 이 프로젝트를 개발한 환경에서는 `python` 명령이 Python 3.9를 가리켜
> `match` 문법에서 오류가 발생합니다. 시스템 기본 Python이 3.10 이상이라면
> `python main.py` 로도 실행할 수 있습니다.

## 사용 예시

프로그램을 실행하면 메인 메뉴가 나타납니다.

```
기본 프롬프트 4개를 불러왔습니다.

========================================
 프롬프트 관리 프로그램
========================================
 1. 프롬프트 추가
 2. 전체 목록 보기
 3. 카테고리별 조회
 4. 키워드 검색
 5. 프롬프트 상세 보기
 6. 즐겨찾기 추가·해제
 7. 즐겨찾기 목록 보기
 0. 프로그램 종료
========================================
번호를 선택하세요:
```

### 전체 목록 보기 (2번)

```
[전체 목록]
번호 | 즐겨찾기 | 제목                           | 카테고리       | 대상 모델
--------------------------------------------------------------------------------
   1 | [*]      | IT 뉴스 요약 및 분류           | 요약·분석      | ChatGPT
   2 | [ ]      | ReqMate 요구사항 문서화        | 기획·문서화    | Claude
   3 | [ ]      | C 언어 코드 오류 분석          | 개발·코딩      | Claude
   4 | [ ]      | 재고 부족 감지 자동화 설계     | 업무 자동화    | ChatGPT
총 4개
```

`[*]`는 즐겨찾기된 프롬프트입니다. 제목이 길면 `...`으로 줄여 표가 깨지지 않게 합니다.

### 프롬프트 추가 (1번)

내용은 여러 줄로 입력할 수 있습니다. 복사해둔 프롬프트를 통째로 붙여넣고
마지막에 `END`만 입력하면 됩니다.

```
[프롬프트 추가]
제목: 영어 이메일 작성
목적: 비즈니스 영어 메일 초안 작성

카테고리를 선택하세요.
 1. 요약·분석
 2. 개발·코딩
 ...
 0. 직접 입력
번호: 4

내용을 입력하세요.
(입력을 마치려면 END만 적고 엔터를 누르세요)
> 아래 내용을 정중한 비즈니스 영어 이메일로 바꿔줘.
>
> - 격식 수준을 상대에 맞게 조절
> - 제목도 함께 제안
>
> 내용: [여기에 붙여넣기]
> END
태그 (쉼표로 구분): 영어, 이메일, 번역
대상 모델 (예: ChatGPT, Claude): ChatGPT
프롬프트가 추가되었습니다. (번호: 5)
```

### 키워드 검색 (4번)

제목·목적·내용·태그를 대소문자 구분 없이 검색합니다.

```
[키워드 검색]
검색어: it

검색어: it
번호 | 즐겨찾기 | 제목                           | 카테고리       | 대상 모델
--------------------------------------------------------------------------------
   1 | [ ]      | IT 뉴스 요약 및 분류           | 요약·분석      | ChatGPT
총 1개
```

### 상세 보기 (5번)

```
[프롬프트 상세 보기]
번호: 1

------------------------------------------------------------
제목     : IT 뉴스 요약 및 분류
카테고리 : 요약·분석
목적     : IT 뉴스의 핵심 내용과 기술 동향 파악
대상 모델: ChatGPT
태그     : IT, 뉴스, 요약, 동향
즐겨찾기 : 아니오
------------------------------------------------------------
프롬프트 내용:
아래 IT 뉴스 기사를 읽고 다음 형식으로 정리해줘.
1. 한 줄 요약
2. 핵심 내용 3가지
3. 관련 기술 분야 분류
4. 개발자 입장에서 주목할 점

기사: [기사 본문을 여기에 붙여넣기]
------------------------------------------------------------
```

## Git 브랜치 작업 설명

기본 브랜치는 `main`이며, **전체 목록 보기 기능**은 별도 브랜치에서 개발한 뒤 병합했습니다.

### 작업 순서

```bash
# 1. main에서 기능 브랜치를 만들고 이동
git checkout -b feature/prompt-list

# 2. 브랜치에서 전체 목록 보기 기능 구현 후 커밋
git add main.py
git commit -m "feat: 전체 프롬프트 목록 보기 기능 구현"

# 3. main으로 돌아와 병합
git checkout main
git merge --no-ff feature/prompt-list
```

### `--no-ff`를 사용한 이유

`--no-ff` 없이 병합하면 Git이 커밋을 일직선으로 이어 붙여
**브랜치를 만들었다는 기록이 그래프에서 사라집니다.**
`--no-ff`를 붙이면 병합 커밋이 따로 생겨 분기와 병합 흔적이 남습니다.

### 결과 그래프

```
*   merge: feature/prompt-list 브랜치 병합
|\
| * feat: 전체 프롬프트 목록 보기 기능 구현   <- feature/prompt-list
|/
*   feat: 프롬프트 추가 기능 구현             <- main
```

그래프는 아래 명령으로 확인할 수 있습니다.

```bash
git log --oneline --graph --decorate --all
```

### 커밋 규칙

기능 단위로 커밋하며, 메시지 앞에 작업 종류를 표시했습니다.

| 접두사 | 의미 |
|---|---|
| `chore:` | 설정·구조 등 기능 외 작업 |
| `feat:` | 새 기능 추가 |
| `test:` | 테스트와 그 과정에서 발견한 수정 |
| `docs:` | 문서 작업 |
| `merge:` | 브랜치 병합 |
